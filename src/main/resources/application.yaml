spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/coffemachine
    username: postgres
    password: postgres
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
  flyway:
    url: jdbc:postgresql://localhost:5432/coffemachine
    user: postgres
    password: postgres
springdoc:
  swagger-ui:
    path: /coffee/swagger-ui.html
  api-docs:
    path: /coffee/api-docs
server:
  port: 8080
